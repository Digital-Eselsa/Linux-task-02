# Project Docs

This directory mirrors `~/project/docs` from the lab.

The practical task requires:
- `report.txt`
- hidden `.secret.txt` initially
- `admin_notes.txt` temporarily, with owner-only read/write permissions, then deletion

Actual lab-generated files may be added here after the practical is performed.
