# Project Bin

This directory mirrors `~/project/bin` from the lab.

The practical task requires `.secret.txt` to be moved here and renamed to:

```text
classified.txt
```

The file must have read permission removed for other users.
