# Project Directory

This directory mirrors the `project` directory that the lab instructs `student01` to create inside the user's home directory.

Required subdirectories:

- `docs/`
- `scripts/`
- `bin/`
- `backup/`

The repository contains placeholders rather than claiming the live Kali Linux lab was completed.
