# Project Backup

This directory mirrors `~/project/backup` from the lab.

The lab requires copying the entire content of `docs` and `scripts` into this directory during the final backup step.

Populate this directory from your actual Kali Linux lab when the backup task is performed.
