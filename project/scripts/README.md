# Project Scripts

This directory mirrors `~/project/scripts` from the lab.

The practical task requires:
- a copy of `report.txt`
- `install.sh` containing the specified Bash commands

Add the actual files from your lab session when completed.
