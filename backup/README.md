# Backup

This directory is reserved for backup-related artifacts associated with the lab.

The practical task specifically requires creating `~/project/backup` and copying the contents of `docs` and `scripts` into it. The repository also mirrors that required location under `project/backup/`.

Do not treat this repository folder as proof that the backup task has been performed in Kali Linux.
