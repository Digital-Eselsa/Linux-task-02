# Commands

This directory is reserved for command-related reference artifacts.

The main command reference is stored at the repository root as `COMMANDS.md`. If you later save terminal transcripts or command logs, place them here and document what they contain.
