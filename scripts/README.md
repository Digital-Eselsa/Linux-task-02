# Scripts

This directory contains scripts explicitly required by the lab.

The lab requires an `install.sh` script containing:

```bash
#!/bin/bash
echo "Installing project dependencies..."
```

The script is intentionally not marked as executed/completed by this repository template. Add the actual lab-created copy if you want to preserve it as evidence.
